# ZA 4b Connected Reading Report

**Date**: 2026-03-15
**Analyst**: Codex (GPT-5)
**Phase**: Month 1 Week 2 - Conservative Admin Stream
**Status**: COMPLETE
**Confidence**: PROBABLE

---

## Executive Summary

- `ZA4b` is the cleanest short Zakros explicit wine record in the current conservative queue: one unidentified opener followed by `VIN 104`.
- The secure result is structural, not lexical: a short opener/label line plus a quantified wine line on a stone vessel.
- The reading extends the single-label commodity template to Zakros and gives the project a high-volume control beside `ZA11a` and `ZA15b`.

---

## Source Information

| Attribute | Value |
|-----------|-------|
| **Tablet ID** | ZA 4b |
| **Site** | Zakros |
| **Period** | Unspecified in corpus metadata |
| **Scribe** | ZA Scribe 1 |
| **Support** | Stone vessel |
| **Document Type** | Short wine record / label-like commodity note |
| **Arithmetic Status** | NO_KURO |
| **Corpus Source** | `data/corpus.json`, `external/lineara/items_analysis/inscriptions.json`, `data/reading_briefs/next_block/ZA4b.json` |
| **Reading Readiness** | Score 0.150 |
| **Cross-Tablet Significance** | Shortest current Zakros explicit `VIN` record; strengthens the single-label commodity template on stone-vessel evidence |

---

## Transliteration

```text
Line 1:  𐝫
Line 2:  VIN  104
```

---

## Anchor Identification

### Level 3: Logograms / Commodity Anchors

| Term | Meaning | Evidence | On this tablet |
|------|---------|----------|----------------|
| **VIN** | wine | standard commodity logogram | Line 2 |

### Level 4: Structural Patterns

| Pattern | Interpretation | Evidence |
|---------|----------------|----------|
| isolated opener line + commodity + integer | short label/account format | `𐝫` followed by `VIN 104` |
| commodity + large integer | quantified short commodity record | `VIN 104` |
| no KU-RO / KI-RO | non-totaled short record | no K-R vocabulary present |

---

## Structural Analysis

`ZA4b` is best treated as a short Zakros wine record on a stone vessel.

The secure core is minimal:

- one isolated unidentified opener on line 1
- one explicit commodity line on line 2: `VIN 104`

This is enough for a bounded connected reading because the second line is fully anchored and the tablet is too short to support a more complex interpretation. The unresolved first-line sign should be treated as an opener, label mark, or control mark, not as a solved lexical item.

The practical importance of `ZA4b` is not that it translates the opener. It is that it adds one more clean Zakros control showing that stone-vessel records can preserve short, high-volume wine entries without KU-RO framing.

---

## Connected Reading Attempt

### Element-by-Element Interpretation

```text
𐝫                 unidentified opener / label mark      UNKNOWN
VIN     104        wine: 104 units                      CERTAIN
```

### Full Interpretive Reading

> [unidentified opener / label]  
> Wine: 104 units

This is a bounded structural reading rather than a sentence translation.

---

## What We Know For Certain

1. `ZA4b` contains one secure `VIN 104` line.
2. The preceding line is a single unresolved sign, not a recoverable word.
3. No KU-RO or KI-RO appears.
4. The inscription is on a Zakros stone vessel, matching other short non-tablet administrative supports at the site.

## What We Hypothesize

1. The first line is functioning as an opener, label, or control mark rather than as a standalone commodity. PROBABLE.
2. `ZA4b` belongs to the same Zakros wine-recording tradition attested in `ZA11a`, `ZA15b`, and `ZA1a`, but in an even more compressed format. PROBABLE.

---

## Cross-Corpus Verification

### Zakros Wine Controls

Relevant completed Zakros controls:

- `ZA11a`: two fractional `VIN` entries (`1/2`, `1/2`) in a mixed-commodity stone-vessel record
- `ZA15b`: `KA-DI | VIN 3`, `KU-RO | VIN 78`, plus `VIN+RA 17` on a stone-vessel summary
- `ZA1a`: large `NI` quantity (`42 1/2`) under the approved wine anchor
- `ZA4b`: `VIN 104` in a two-line stone-vessel note

Result:

- Zakros wine accounting appears in both expanded and extremely compressed formats
- `ZA4b` is the cleanest current Zakros control for a bare high-volume `VIN` line without totals or subordinate entries

### Template Verification

`ZA4b` fits the project's short commodity-record template:

- `KN Zb 27`: resolved label + `VIN 17`
- `ZA4b`: unresolved opener + `VIN 104`

Result:

- the single-label commodity template remains valid even when the label element itself is not deciphered
- the secure reading floor here comes from the commodity line, not from speculative recovery of the opener

### Academic-Overlap Guard

Local overlap check for this pass found:

- no prior completed project reading for `ZA4b`
- no dedicated local reference note overriding the corpus-based interpretation

This report therefore adds a repo-native structural reading rather than restating an existing project conclusion.

---

## Promotion Recommendation

**Decision**: `MAINTAIN`

Rationale:

1. No new lexical promotion is proposed.
2. `VIN` is already certain; the unresolved opener remains unresolved.
3. The value of `ZA4b` is structural: it strengthens the Zakros wine dossier and the short-record admin template.

---

## Final Assessment

`ZA4b` is a small but high-value control reading. It shows that Zakros stone-vessel administration can compress a wine record down to an unidentified opener plus a single anchored quantity line, and it does so at substantial scale (`VIN 104`). The result is modest, but credible and reusable.
